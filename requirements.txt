# ARC Solver Evaluation Platform — runtime dependencies
# Install: pip install -r requirements.txt
# Python >= 3.11 (see pyproject.toml)

pandas==2.3.3
numpy==2.3.5
pyarrow==21.0.0
duckdb==1.5.4
matplotlib==3.10.6
requests==2.32.5
tabulate==0.9.0
